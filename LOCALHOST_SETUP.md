# 🏠 Настройка Supabase для localhost разработки

## 📋 **Что нужно настроить в Supabase Dashboard:**

### **1. 🔗 Site URL:**
- Перейдите в **Authentication → URL Configuration**
- Оставьте **Site URL** вашим прод-доменом (например, `https://lvlmart.kz`)

### **2. 🔄 Redirect URLs:**
Добавьте локальные адреса в **Redirect URLs** (Next.js dev на 3000):
```
http://localhost:3000/auth/callback
http://localhost:3000/auth/callback?type=signup
http://localhost:3000/auth/callback?type=magiclink
http://localhost:3000/auth/callback?type=recovery
http://localhost:3000/auth/callback?type=invite
http://localhost:3000/auth/callback?type=email_change
http://localhost:3000/reset-password
```
Дополнительно (если используете 127.0.0.1):
```
http://127.0.0.1:3000/auth/callback
http://127.0.0.1:3000/reset-password
```

### **3. 📧 Email Templates:**

#### **Password Reset Template:**
- **Subject**: `Сброс пароля для lvlmart (localhost)`
- **Body**:
```html
<h2>Сброс пароля</h2>
<p>Вы запросили сброс пароля для вашего аккаунта в lvlmart.</p>
<p>Нажмите на ссылку ниже, чтобы сбросить пароль:</p>
<p><a href="{{ .ConfirmationURL }}">Сбросить пароль</a></p>
<p>Если вы не запрашивали сброс пароля, проигнорируйте это письмо.</p>
<p>Ссылка действительна в течение 24 часов.</p>
<p><small>Это тестовое письмо для localhost разработки</small></p>
```

#### **Email Confirmation Template:**
- **Subject**: `Подтверждение email для lvlmart (localhost)`
- **Body**:
```html
<h2>Подтверждение email</h2>
<p>Добро пожаловать в lvlmart!</p>
<p>Нажмите на ссылку ниже, чтобы подтвердить ваш email:</p>
<p><a href="{{ .ConfirmationURL }}">Подтвердить email</a></p>
<p>Если вы не создавали аккаунт, проигнорируйте это письмо.</p>
<p><small>Это тестовое письмо для localhost разработки</small></p>
```

### **4. 🚀 Запуск проекта:**

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите заново:
npm run dev
```

Проект будет доступен по адресу: **http://localhost:3000**

---

## ⚠️ **Важные замечания:**

1. **Site URL** остаётся прод-доменом. Для локалки используем только Redirect URLs + код передаёт `redirectTo` на основе `window.location.origin`.
2. **Тестовые данные**: Используйте тестовые email адреса для проверки
3. **Переключение**: Для продакшена убедитесь, что добавлены прод Redirect URLs (`https://lvlmart.kz/auth/callback`, `https://lvlmart.kz/reset-password`).

---

## 🧪 **Тестирование:**

1. Откройте http://localhost:3000
2. Попробуйте зарегистрироваться
3. Проверьте сброс пароля
4. Убедитесь, что все работает корректно

**Готово! Теперь вы можете разрабатывать на localhost!** 🎉

